// Start coding here
let calculator = {
    add : (a,b) => a+b,
    subtract : (a,b) => a-b,
    multiply : (a,b) => a*b,
    devide : (a,b) => a/b
};

const addResult = calculator.add(10,20);
console.log(addResult);

const devideResult = calculator.devide(3000,10);
console.log(devideResult);